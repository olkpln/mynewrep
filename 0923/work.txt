для НОВОЙ ветки:
git checkout -b <имя ветки>
для существующей или новой, которая есть на удаленном репо:
git checkout <имя ветки>

git push -u origin testbranch
аналогично
git push --set-upstream origin testbranch

git clone https://github.com/ShellKhan/learning1146.git
git pull
git checkout testbranch

git remote remove origin
git remote add origin https://github.com/ShellKhan/tst.git
git push -u origin testbranch



1) коммитим изменения в памятке в каталоге 0923
2) пробуем запушить их по старому ориджину
	не получается
3) меняем ориджин на новый:
	- создаем новый репо на гитхабе
	- у себя в репо подаем команды:
		git remote remove origin
		git remote add origin (новый адрес с гитхаба)
		git push -u origin (имя ветки)
	- переключаемся на ветку master, и ее тоже апстримим
	- проверяем на гитхабе результат
4) устанавливаем дефолтной на гитхабе ветку мастер



создали файл в ветке мастер
создали/переключились на ветку testmerge
	git checkout -b <имя ветки>
				 -b - параметр для создания ветки
изменили файл
сделали коммит
запушили с апстримом на гитхаб
переключились на мастер
изменили файл
сделали коммит
запушили на гитхаб
оставаясь на мастере, пытаемся вмержить в него тестмерж
решаем мерж-конфликт
коммитим результат
пушим на гитхаб


git status > <имя файла>
git log > <имя файла>
git show <идентификатор коммита (достаточно 7-8 цифр)> > <имя файла>
git blame <имя файла> > <имя файла>
только для незакоммиченных изменений (для закоммиченных см. документацию гита):
git diff > <имя файла>






